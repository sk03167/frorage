{
  "name": "frorage",
  "private": true,
  "workspaces": [
    "apps/web",
    "packages/sdk"
  ],
  "scripts": {
    "test": "npm -w @frorage/sdk test",
    "dev:web": "npm -w @frorage/web run dev",
    "build": "npm -w @frorage/sdk run build && npm -w @frorage/web run build"
  },
  "devDependencies": {
    "@types/node": "^25.9.1"
  }
}
